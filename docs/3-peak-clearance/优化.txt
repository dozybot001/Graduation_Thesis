clear;clc;close all;

x=sdpvar(1,2);
C=[
   x(1)+x(2)>=2
   x(2)-x(1)<=1
   x(1)<=1
   ];

ops=sdpsettings('verbose',0,'solver','cplex');

z=-(x(1)+2*x(2))/(2*x(1)+x(2));

result=optimize(C,z);
if result.problem==0
	value(x)
	-value(z)
else disp('Error');
end
